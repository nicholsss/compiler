

# def parse_expression() -> ast.Expression:
#   left = parse_literal()
#  op_token = consume()
# if op_token.text not in ['+', '-', '*', '/']:
#    raise Exception('Expected an operator', {op_token})

# right = parse_literal()
# return ast.BinaryOperator(left=left, op=op_token.text, right=right)
